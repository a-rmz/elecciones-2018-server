<p id="notice"><%= notice %></p>

<h1>Pages</h1>

<table class="table">
  <thead>
    <tr>
      <th>Id</th>
      <th>Slug</th>
      <th>Name</th>
      <th>Stats 📈</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= page.id %></td>
        <td><a href="https://facebook.com/<%= page.slug_id %>"><%= page.slug_id %></a></td>
        <td><%= page.name %></td>
        <td>
        <table class="table bordered">
          <thead>
            <th>Count</th>
            <th>Date 📅</th>
          </thead>
          <tbody>
            <% page.fan_count.each do |record| %>
              <tr>
                <td><%= record['count'] %></td>
                <td><%= record['date'] %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <td><%= link_to 'Show', page %></td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= paginate @pages %>
<%= link_to 'Home', '/' %>
